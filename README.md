# airline_sort_program
 Python application for analyzing flight data. Loads CSV files containing airline schedules and prices. Features include flight search, duration filtering, price comparison, departure time queries, average cost calculation, and bubble sort export functionality.
